# Environment variables for local development
NODE_ENV=development
PORT=3000

# Tap Payment Integration - TEST KEYS ONLY (safe for development)
TAP_SECRET_KEY=sk_test_REPLACE_WITH_YOUR_TEST_KEY
ADMIN_TOKEN=admin123
FRONTEND_URL=http://localhost:3000

# JWT Authentication - Development secrets
JWT_SECRET=dev-jwt-secret-change-in-production
SESSION_SECRET=dev-session-secret-change-in-production

# For local development, we'll use SQLite instead of PostgreSQL
# DATABASE_URL is not needed for SQLite

# If you want to use PostgreSQL locally, uncomment and update:
# DATABASE_URL=postgresql://user:password@localhost:5432/techpartner_local

# If you want to use PostgreSQL, uncomment and update the line below:
# DATABASE_URL=postgresql://user:password@localhost:5432/techpartner_local
